import { createTheme } from "@mui/material";

export const StandardTheme = createTheme({
    palette: {
        primary: {
            main: "#ecac0e",
            light: "#f0cf51",
            dark: "#ea6d00",
            contrastText: "#3c3c3c"
        }
    }
})